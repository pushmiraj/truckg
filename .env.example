# Backend environment variables
# Copy this file to .env and fill in your values

# Optional — if unset the API runs in Dev Bypass mode (no auth required)
SUPABASE_JWT_SECRET=your_supabase_jwt_secret_here
